# Parent profile
quarkus.config.profile.parent=common

# Logging configuration
quarkus.log.category."com.hiperium".level=DEBUG

# Database configuration
quarkus.datasource.username=admin
quarkus.datasource.password=admin123
quarkus.datasource.jdbc.url=jdbc:postgresql://localhost:5432/timer_service

# AWS configuration
quarkus.dynamodb.aws.credentials.type=env-variable